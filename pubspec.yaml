name: nivvesh
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.16
  carousel_slider: ^5.0.0
  smooth_page_indicator: ^1.2.0+3
  google_fonts: ^6.2.1
  shimmer_animation: ^2.2.1
  get_it: ^8.0.3
  dio: ^5.7.0
  dartz: ^0.10.1
  flutter_bloc: ^8.1.6
  flutter_native_splash: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^4.0.0

flutter_native_splash:
  android: true
  ios: true
  background_image: assets/nivvesh_splash.png
  fullscreen: true
  android_gravity: fill
  android_12:
    image: assets/nivvesh_logo.png
    color: "#0F1115"

    # TODO
    # App icon without an icon background: This should be 1152×1152 pixels, and fit within a circle
    # 768 pixels in diameter.  To fit a 1152x1152 image within a circle with a 768 diameter, simply 
    # ensure that the most important design elements of your image are placed within a circular area
    # with a 768 diameter at the center of the 1152x1152 canvas.
    

flutter:

  uses-material-design: true
  assets:
    - assets/images/navbar/
    - assets/images/banks/
