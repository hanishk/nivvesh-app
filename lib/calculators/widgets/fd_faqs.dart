import 'package:flutter/material.dart';

class FDFaqs extends StatelessWidget {
  const FDFaqs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SingleChildScrollView(
        child: Text(''),
      ),
    );
  }
}
